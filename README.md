# mongoAssessment
MongoDB with AI Assessment 

npm init -y
npm install express dotenv cors mongodb @google/genai
npm install -g nodemon

# for hash password
npm install bcrypt 

# use JWT
npm install jsonwebtoken

# generate secret key and included in .env file as TOKEN_SECRET
node generateSecret.js
